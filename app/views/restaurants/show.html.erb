<h1><%= @restaurant.name %></h1>
<h2><%= @restaurant.rating %> stars </h2>
<h3><%= @restaurant.address %></h3>


<%= link_to "Delete #{@restaurant.name}",
            restaurant_path(@restaurant),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<%= link_to "Edit", edit_restaurant_path(@restaurant)%>
